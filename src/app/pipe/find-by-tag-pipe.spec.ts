import { FindByTagPipe } from './find-by-tag-pipe';

describe('FindByTagPipe', () => {
  it('create an instance', () => {
    const pipe = new FindByTagPipe();
    expect(pipe).toBeTruthy();
  });
});
